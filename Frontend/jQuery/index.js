$(document).ready(function() { // once our document is ready, then it will call this code.
    $("h1").css("color", "red");
});
// to keep track of until our jQuery library has finished loading.
// this is done when both script tags are in header below link tag
// so its alternative is to place script tags before the end of body tag